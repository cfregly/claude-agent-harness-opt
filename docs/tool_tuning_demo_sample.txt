$ python -m claude_agent_harness_opt optimize-tools evals/examples/tool_tuning_before_bundle.json --markdown || true
# Tool Selection Optimizer

Passed: no
Score: 0.018

Recommendations
- Add an input_schema or parameters block to search so the agent can form valid calls.
- Add tool_selection_cases with expected and forbidden tools for confusing boundaries.
- Add heldout_tool_selection_cases before promoting a tool-description change.
- Add context_controls or input parameters like limit, filter, page_size, range, or response_format.
- Add quality_checks so the agent knows how to inspect results before the next call.
- Add tool_metrics so runtime, tool-call count, tokens, and tool errors are visible.

$ python -m claude_agent_harness_opt optimize-tools evals/examples/agent_audit_bundle.json --markdown
# Tool Selection Optimizer

Passed: yes
Score: 1.000

Recommendations
- No required changes.

What changed
- search became web_search: discovery only, with query schema and avoid rules.
- fetching moved to web_fetch: known URL only, with source and truncation checks.
- math moved to calculator: deterministic expression only, with error guidance.
- calibration and held-out cases prove the routing boundary before access widens.
